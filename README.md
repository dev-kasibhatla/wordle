# Wordle

Modular Wordle application with:
- one FastAPI endpoint
- a Wordle-verification engine
- a two-mode solver
- an asyncio batch evaluator with report artifacts

Use `uv` for setup and execution.
